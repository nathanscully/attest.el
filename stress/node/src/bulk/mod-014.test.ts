import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 014", () => {
  it("check 01", () => {
    assert.equal(1 * 14, 14);
  });
  it("check 02", () => {
    assert.equal(2 * 14, 28);
  });
  it("check 03", () => {
    assert.equal(3 * 14, 42);
  });
  it("check 04", () => {
    assert.equal(4 * 14, 56);
  });
  it("check 05", () => {
    assert.equal(5 * 14, 70);
  });
  it("check 06", () => {
    assert.equal(6 * 14, 84);
  });
  it("check 07", () => {
    assert.equal(7 * 14, 98);
  });
  it("check 08", () => {
    assert.equal(8 * 14, 113);
  });
  it("check 09", () => {
    assert.equal(9 * 14, 126);
  });
  it("check 10", () => {
    assert.equal(10 * 14, 140);
  });
  it("check 11", () => {
    assert.equal(11 * 14, 154);
  });
  it("check 12", () => {
    assert.equal(12 * 14, 168);
  });
  it("check 13", () => {
    assert.equal(13 * 14, 182);
  });
  it("check 14", () => {
    assert.equal(14 * 14, 196);
  });
  it("check 15", () => {
    assert.equal(15 * 14, 210);
  });
});

describe("module 014 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
