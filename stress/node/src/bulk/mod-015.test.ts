import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 015", () => {
  it("check 01", () => {
    assert.equal(1 * 15, 15);
  });
  it("check 02", () => {
    assert.equal(2 * 15, 30);
  });
  it("check 03", () => {
    assert.equal(3 * 15, 45);
  });
  it("check 04", () => {
    assert.equal(4 * 15, 60);
  });
  it("check 05", () => {
    assert.equal(5 * 15, 75);
  });
  it("check 06", () => {
    assert.equal(6 * 15, 90);
  });
  it("check 07", () => {
    assert.equal(7 * 15, 105);
  });
  it("check 08", () => {
    assert.equal(8 * 15, 120);
  });
  it("check 09", () => {
    assert.equal(9 * 15, 135);
  });
  it("check 10", () => {
    assert.equal(10 * 15, 150);
  });
  it("check 11", () => {
    assert.equal(11 * 15, 165);
  });
  it("check 12", () => {
    assert.equal(12 * 15, 180);
  });
  it("check 13", () => {
    assert.equal(13 * 15, 195);
  });
  it("check 14", () => {
    assert.equal(14 * 15, 210);
  });
  it("check 15", () => {
    assert.equal(15 * 15, 225);
  });
});

describe("module 015 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
