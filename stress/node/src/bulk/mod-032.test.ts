import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 032", () => {
  it("check 01", () => {
    assert.equal(1 * 32, 32);
  });
  it("check 02", () => {
    assert.equal(2 * 32, 64);
  });
  it("check 03", () => {
    assert.equal(3 * 32, 96);
  });
  it("check 04", () => {
    assert.equal(4 * 32, 128);
  });
  it("check 05", () => {
    assert.equal(5 * 32, 160);
  });
  it("check 06", () => {
    assert.equal(6 * 32, 192);
  });
  it("check 07", () => {
    assert.equal(7 * 32, 224);
  });
  it("check 08", () => {
    assert.equal(8 * 32, 256);
  });
  it("check 09", () => {
    assert.equal(9 * 32, 288);
  });
  it("check 10", () => {
    assert.equal(10 * 32, 320);
  });
  it("check 11", () => {
    assert.equal(11 * 32, 352);
  });
  it("check 12", () => {
    assert.equal(12 * 32, 384);
  });
  it("check 13", () => {
    assert.equal(13 * 32, 416);
  });
  it("check 14", () => {
    assert.equal(14 * 32, 448);
  });
  it("check 15", () => {
    assert.equal(15 * 32, 480);
  });
});

describe("module 032 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
