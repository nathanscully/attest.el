import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 030", () => {
  it("check 01", () => {
    assert.equal(1 * 30, 30);
  });
  it("check 02", () => {
    assert.equal(2 * 30, 60);
  });
  it("check 03", () => {
    assert.equal(3 * 30, 90);
  });
  it("check 04", () => {
    assert.equal(4 * 30, 120);
  });
  it("check 05", () => {
    assert.equal(5 * 30, 150);
  });
  it("check 06", () => {
    assert.equal(6 * 30, 180);
  });
  it("check 07", () => {
    assert.equal(7 * 30, 210);
  });
  it("check 08", () => {
    assert.equal(8 * 30, 240);
  });
  it("check 09", () => {
    assert.equal(9 * 30, 270);
  });
  it("check 10", () => {
    assert.equal(10 * 30, 300);
  });
  it("check 11", () => {
    assert.equal(11 * 30, 330);
  });
  it("check 12", () => {
    assert.equal(12 * 30, 360);
  });
  it("check 13", () => {
    assert.equal(13 * 30, 390);
  });
  it("check 14", () => {
    assert.equal(14 * 30, 420);
  });
  it("check 15", () => {
    assert.equal(15 * 30, 450);
  });
});

describe("module 030 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
