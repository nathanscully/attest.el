import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 020", () => {
  it("check 01", () => {
    assert.equal(1 * 20, 20);
  });
  it("check 02", () => {
    assert.equal(2 * 20, 40);
  });
  it("check 03", () => {
    assert.equal(3 * 20, 60);
  });
  it("check 04", () => {
    assert.equal(4 * 20, 80);
  });
  it("check 05", () => {
    assert.equal(5 * 20, 100);
  });
  it("check 06", () => {
    assert.equal(6 * 20, 120);
  });
  it("check 07", () => {
    assert.equal(7 * 20, 140);
  });
  it("check 08", () => {
    assert.equal(8 * 20, 160);
  });
  it("check 09", () => {
    assert.equal(9 * 20, 180);
  });
  it("check 10", () => {
    assert.equal(10 * 20, 200);
  });
  it("check 11", () => {
    assert.equal(11 * 20, 220);
  });
  it("check 12", () => {
    assert.equal(12 * 20, 240);
  });
  it("check 13", () => {
    assert.equal(13 * 20, 260);
  });
  it("check 14", () => {
    assert.equal(14 * 20, 280);
  });
  it("check 15", () => {
    assert.equal(15 * 20, 300);
  });
});

describe("module 020 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
