import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 002", () => {
  it("check 01", () => {
    assert.equal(1 * 2, 2);
  });
  it("check 02", () => {
    assert.equal(2 * 2, 4);
  });
  it("check 03", () => {
    assert.equal(3 * 2, 6);
  });
  it("check 04", () => {
    assert.equal(4 * 2, 8);
  });
  it("check 05", () => {
    assert.equal(5 * 2, 10);
  });
  it("check 06", () => {
    assert.equal(6 * 2, 12);
  });
  it("check 07", () => {
    assert.equal(7 * 2, 14);
  });
  it("check 08", () => {
    assert.equal(8 * 2, 16);
  });
  it("check 09", () => {
    assert.equal(9 * 2, 18);
  });
  it("check 10", () => {
    assert.equal(10 * 2, 20);
  });
  it("check 11", () => {
    assert.equal(11 * 2, 22);
  });
  it("check 12", () => {
    assert.equal(12 * 2, 24);
  });
  it("check 13", () => {
    assert.equal(13 * 2, 26);
  });
  it("check 14", () => {
    assert.equal(14 * 2, 28);
  });
  it("check 15", () => {
    assert.equal(15 * 2, 30);
  });
});

describe("module 002 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
