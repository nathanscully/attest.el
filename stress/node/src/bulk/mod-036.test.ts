import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 036", () => {
  it("check 01", () => {
    assert.equal(1 * 36, 36);
  });
  it("check 02", () => {
    assert.equal(2 * 36, 72);
  });
  it("check 03", () => {
    assert.equal(3 * 36, 108);
  });
  it("check 04", () => {
    assert.equal(4 * 36, 144);
  });
  it("check 05", () => {
    assert.equal(5 * 36, 180);
  });
  it("check 06", () => {
    assert.equal(6 * 36, 216);
  });
  it("check 07", () => {
    assert.equal(7 * 36, 252);
  });
  it("check 08", () => {
    assert.equal(8 * 36, 288);
  });
  it("check 09", () => {
    assert.equal(9 * 36, 324);
  });
  it("check 10", () => {
    assert.equal(10 * 36, 360);
  });
  it("check 11", () => {
    assert.equal(11 * 36, 396);
  });
  it("check 12", () => {
    assert.equal(12 * 36, 432);
  });
  it("check 13", () => {
    assert.equal(13 * 36, 468);
  });
  it("check 14", () => {
    assert.equal(14 * 36, 504);
  });
  it("check 15", () => {
    assert.equal(15 * 36, 540);
  });
});

describe("module 036 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
