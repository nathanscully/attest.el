import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("module 025", () => {
  it("check 01", () => {
    assert.equal(1 * 25, 25);
  });
  it("check 02", () => {
    assert.equal(2 * 25, 50);
  });
  it("check 03", () => {
    assert.equal(3 * 25, 75);
  });
  it("check 04", () => {
    assert.equal(4 * 25, 100);
  });
  it("check 05", () => {
    assert.equal(5 * 25, 125);
  });
  it("check 06", () => {
    assert.equal(6 * 25, 150);
  });
  it("check 07", () => {
    assert.equal(7 * 25, 175);
  });
  it("check 08", () => {
    assert.equal(8 * 25, 200);
  });
  it("check 09", () => {
    assert.equal(9 * 25, 225);
  });
  it("check 10", () => {
    assert.equal(10 * 25, 250);
  });
  it("check 11", () => {
    assert.equal(11 * 25, 275);
  });
  it("check 12", () => {
    assert.equal(12 * 25, 300);
  });
  it("check 13", () => {
    assert.equal(13 * 25, 325);
  });
  it("check 14", () => {
    assert.equal(14 * 25, 350);
  });
  it("check 15", () => {
    assert.equal(15 * 25, 375);
  });
});

describe("module 025 extras", () => {
  it("extra 01", () => {
    assert.ok(1);
  });
  it("extra 02", () => {
    assert.ok(2);
  });
  it("extra 03", () => {
    assert.ok(3);
  });
  it("extra 04", () => {
    assert.ok(4);
  });
  it("extra 05", () => {
    assert.ok(5);
  });
  it("extra 06", () => {
    assert.ok(6);
  });
  it("extra 07", () => {
    assert.ok(7);
  });
  it("extra 08", () => {
    assert.ok(8);
  });
  it("extra 09", () => {
    assert.ok(9);
  });
  it("extra 10", () => {
    assert.ok(10);
  });
});
